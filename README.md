# Stock Volatility Tracker (주식 변동성 추적기)

주식의 일일 변동률을 추적하고 통계적 시그마(표준편차) 값을 계산하는 도구입니다.

## 기능

- 주식 티커를 입력받아 최근 1년간의 일일 데이터 수집
- 전일 대비 변동률(%) 계산
- 변동률의 평균과 표준편차 계산
- 1σ, 2σ, 3σ 구간 계산 및 출력
- 내일 몇 %가 변하면 각 시그마를 넘는지 예측
- **SQLite 캐싱**: 다운로드한 데이터와 분석 결과를 로컬에 저장하여 빠른 재분석 가능
- 명령줄 인자로 티커 직접 입력 지원
- 캐시 새로고침 및 통계 보기 기능

## 설치 방법

```bash
pip install -r requirements.txt
```

## 사용 방법

### 실제 주식 데이터로 실행

**방법 1: 명령줄 인자로 티커 입력 (권장)**

```bash
# 기본 사용 (최근 1년 데이터, 캐시 자동 사용)
python sigma_calculator.py AAPL

# 기간 지정 (최근 180일 데이터)
python sigma_calculator.py TSLA -d 180

# 한국 주식
python sigma_calculator.py 005930.KS

# 캐시 무시하고 새로 다운로드
python sigma_calculator.py AAPL --refresh

# 캐시 사용하지 않기
python sigma_calculator.py AAPL --no-cache

# 캐시 통계 보기
python sigma_calculator.py --stats

# 도움말 보기
python sigma_calculator.py --help
```

**방법 2: 대화형 모드**

```bash
python sigma_calculator.py
```

실행 후 주식 티커를 입력하세요:
- 미국 주식: `AAPL`, `TSLA`, `GOOGL` 등
- 한국 주식: `005930.KS` (삼성전자), `000660.KS` (SK하이닉스) 등

### 캐시 기능

프로그램은 SQLite 데이터베이스에 데이터를 자동으로 캐싱합니다:
- **첫 실행**: 데이터를 다운로드하고 캐시에 저장 (약간 느림)
- **두 번째 실행**: 캐시에서 즉시 불러옴 (매우 빠름)
- **캐시 갱신**: `--refresh` 옵션으로 최신 데이터 다운로드
- **캐시 통계**: `--stats` 옵션으로 저장된 데이터 확인

### 샘플 데이터로 테스트

네트워크 문제나 API 접근 제한이 있는 경우, 샘플 데이터로 프로그램을 테스트할 수 있습니다:

```bash
# 기본 샘플 데이터 (252일)
python test_sample.py

# 티커와 기간 지정
python test_sample.py -t AAPL -d 180
```

## 예시

```
주식 티커를 입력하세요 (예: AAPL, TSLA, 005930.KS): AAPL

📊 AAPL 데이터를 다운로드하는 중...
✅ 252일의 데이터를 다운로드했습니다.
✅ 251개의 일일 변동률을 계산했습니다.
✅ 통계 계산 완료
   평균 변동률: 0.1234%
   표준편차(1σ): 1.8765%

============================================================
📈 AAPL 주식 변동성 분석 리포트
============================================================

📅 분석 기간: 최근 365일
📊 분석 데이터: 251일의 일일 변동률

📉 기본 통계:
   평균 일일 변동률: +0.1234%
   표준편차 (1σ): 1.8765%

🎯 내일 예상 변동 범위:

   1 시그마 (68.3% 확률):
      상승: +2.00% 이상
      하락: -1.75% 이하

   2 시그마 (95.4% 확률):
      상승: +3.88% 이상
      하락: -3.63% 이하

   3 시그마 (99.7% 확률):
      상승: +5.75% 이상
      하락: -5.50% 이하
```

## 시그마(σ) 해석

- **1σ**: 정규분포에서 약 68.3%의 데이터가 이 범위 안에 존재
  - 내일 변동률이 이 범위를 벗어날 확률: 약 31.7%

- **2σ**: 정규분포에서 약 95.4%의 데이터가 이 범위 안에 존재
  - 내일 변동률이 이 범위를 벗어날 확률: 약 4.6%

- **3σ**: 정규분포에서 약 99.7%의 데이터가 이 범위 안에 존재
  - 내일 변동률이 이 범위를 벗어날 확률: 약 0.3% (매우 드묾)

## 프로그래밍 방식 사용

```python
from sigma_calculator import StockVolatilityTracker

# 추적기 생성
tracker = StockVolatilityTracker('AAPL', period_days=365)

# 전체 분석 실행
tracker.analyze()

# 또는 단계별 실행
tracker.fetch_data()
tracker.calculate_daily_returns()
tracker.calculate_sigma()

# 시그마 레벨 가져오기
sigma_levels = tracker.get_sigma_levels()
print(sigma_levels)
```

## 주의사항

- 과거 데이터를 기반으로 한 통계이므로 미래를 보장하지 않습니다
- 블랙스완 이벤트(예상치 못한 큰 변동)는 예측할 수 없습니다
- 투자 결정은 본인의 판단과 책임 하에 하시기 바랍니다

## 라이선스

MIT License
